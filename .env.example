# Copy to .env and fill in. .env is gitignored — never commit it.
#
# Only needed if you still use the legacy deploy.py push path.
# The git + PR workflow (see CLAUDE.md → Deployment) does not read this file;
# it authenticates through the gh CLI or your git credential helper instead.
GH_TOKEN=ghp_yourtokenhere
